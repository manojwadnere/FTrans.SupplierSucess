CREATE TABLE [dbo].[FileType] (
    [FileTypeID]          SMALLINT      NOT NULL,
    [FileTypeName]        VARCHAR (200) NULL,
    [FileTypeDescription] VARCHAR (500) NULL,
    PRIMARY KEY CLUSTERED ([FileTypeID] ASC)
);

